// Tag It "Sunset Pop" brand gradient. Applied via inline styles (React style
// prop) rather than a CSS utility class, so it renders reliably regardless of
// Tailwind's class generation.
export const BRAND_GRADIENT = "linear-gradient(135deg, #ff9f45, #ff5e7e 55%, #e23670)";
